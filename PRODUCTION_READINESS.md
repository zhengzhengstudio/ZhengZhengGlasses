# 铮铮眼镜正式版扩容路线

## 当前可上线范围

当前版本适合比赛展示、邀请测试和小规模真实用户。人脸关键点识别在用户设备本地运行，能减少服务器计算压力；镜框静态资源可交给 CDN 缓存。

服务端聊天、会话和部分商家数据目前仍通过 JSON 文件读写。该方式不能用于大规模并发，正式扩大用户量前必须迁移。

## 扩容优先级

### P0：公开上线前

- 使用 PostgreSQL 保存用户、商家、会话、消息、订单和配镜记录。
- 使用对象存储保存试戴图、聊天图片、镜框原图和 3D 模型，数据库只保存 URL。
- 所有图片上传使用签名直传，禁止把 Base64 图片写入 JSON 数据库或聊天消息。
- 静态网站、镜框图、MediaPipe 模型和 WASM 文件使用 CDN，并设置长缓存和版本化 URL。
- API 增加身份验证、按用户和 IP 限流、请求体大小限制、审计日志。
- 配置错误监控、接口延迟监控、健康检查和自动备份。

### P1：用户增长阶段

- 聊天由短轮询升级为 WebSocket 或 Server-Sent Events。
- 使用 Redis 保存在线状态、限流计数、热点目录缓存和任务状态。
- Kimi、3D 建模、图片处理改为异步任务队列，API 立即返回任务 ID。
- 镜框目录使用分页、搜索索引和增量更新，不允许一次返回完整镜框库。
- 将 API 服务做成无状态服务，支持多实例和负载均衡。

### P2：大规模阶段

- 数据库读写分离、分区和归档；消息表按会话或时间分区。
- 多地域 CDN、对象存储和容灾备份。
- 为 MediaPipe、试戴、聊天、下单建立独立的性能指标和容量告警。
- 建立灰度发布、自动回滚、压测和灾难恢复演练。

## 建议容量门槛

- 公开测试前：完成 100 个并发用户、持续 30 分钟的压测。
- 扩大推广前：完成 1,000 个并发用户、聊天和目录接口 P95 小于 500ms。
- 大规模宣传前：使用生产数据库、对象存储、CDN、消息通道和多实例 API，不再使用 JSON 文件作为在线数据源。

## 隐私原则

- 人脸照片默认只在本机处理，上传必须获得明确同意。
- 报告与商家沟通优先传参数，不默认传原始人脸照片。
- 提供删除照片、删除记录和撤回商家访问权限的入口。
- 未成年人使用时应由监护人确认，并避免公开展示真实人脸。
